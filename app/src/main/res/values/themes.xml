<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CafeSpot" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>

    <style name="StartButtonStyle">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">24sp</item>
        <item name="android:backgroundTint">@color/brown</item>
        <item name="android:layout_marginTop">16dp</item>
        <item name="android:radius">0dp</item>
    </style>

    <style name="Theme.CafeSpot" parent="Base.Theme.CafeSpot" />
</resources>
